# Dagster Instance Configuration

# Run storage, event log, schedule storage → PostgreSQL
storage:
  postgres:
    postgres_url:
      env: DAGSTER_PG_CONN_STRING

run_launcher:
  module: dagster.core.launcher
  class: DefaultRunLauncher

# No telemetry for local development
telemetry:
  enabled: false
